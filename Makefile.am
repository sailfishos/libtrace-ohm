SUBDIRS        = rewrite rewrite/tests

#pkgconfigdir   = ${libdir}/pkgconfig
#pkgconfig_DATA = src/libtrace.pc

MAINTAINTERCLEANFILES = Makefile.in

clean-local:
	find . -name \*~ -exec rm -f {} \;

